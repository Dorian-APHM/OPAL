# OPAL Environment Configuration
# Copy this file to .env and adjust values for your environment.

# --- Security (REQUIRED) ---
# Generate with: openssl rand -hex 32
SECRET_KEY=

# --- Database ---
POSTGRES_USER=opal
POSTGRES_PASSWORD=opal
POSTGRES_DB=opal

# --- Authentication ---
AUTH_ENABLED=true
KEYCLOAK_REALM=opal
KEYCLOAK_CLIENT_ID=opal-frontend
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# --- Networking ---
# Hostname/IP for Keycloak (used by clients to reach the auth server)
KC_HOSTNAME=localhost
KC_HOSTNAME_PORT=8080
KEYCLOAK_PORT=8080
FRONTEND_PORT=3000

# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# --- OHDSI Integration (optional) ---
OHDSI_IMAGE_PREFIX=ohdsi-docker
OHDSI_HOST_OUTPUT_DIR=
OHDSI_HOST_SCRIPTS_DIR=
OHDSI_HOST_JDBC_DIR=/data/jdbc_drivers
