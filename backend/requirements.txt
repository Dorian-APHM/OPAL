fastapi==0.135.1
uvicorn[standard]==0.42.0
sqlalchemy==2.0.48
psycopg2-binary==2.9.11
cryptography==46.0.7
pydantic==2.12.5
httpx==0.28.1
# Used by admin_router for the Keycloak admin REST API. It used to be pulled in
# transitively by the `docker` package (removed with the OHDSI socket refactor),
# so it must now be declared explicitly.
requests==2.32.3
PyJWT[crypto]==2.12.1
python-multipart==0.0.27
slowapi==0.1.9
alembic==1.18.4
